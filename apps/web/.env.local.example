# Supabase (local dev — fill from `npx supabase start` output)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Worker chat endpoint (Phase 6) — dev default points at localhost worker.
WORKER_CHAT_URL=http://127.0.0.1:4000/chat

# Google OAuth (after Task 11)
# These go into supabase/config.toml [auth.external.google] via env vars,
# NOT into this file directly.
